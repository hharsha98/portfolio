---
import '../styles/global.css'
import Header from '../components/Header.astro'
import Footer from '../components/Footer.astro'
import { site, evehicleshop } from '../data/site'

interface Props {
  title?: string
  description?: string
  canonical?: string
}

const title = Astro.props.title ?? site.title
const description = Astro.props.description ?? site.description
const canonical = new URL(Astro.props.canonical ?? Astro.url.pathname, site.url).toString()
const photo = new URL(site.photo, site.url).toString()
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: site.name,
  jobTitle: site.role,
  email: site.email,
  url: site.url,
  image: photo,
  address: {
    '@type': 'PostalAddress',
    addressLocality: 'Nuremberg',
    addressCountry: 'DE',
  },
  alumniOf: {
    '@type': 'CollegeOrUniversity',
    name: 'Friedrich-Alexander-Universität Erlangen-Nürnberg',
  },
  sameAs: [site.github, site.linkedin, site.huggingface, evehicleshop.href],
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#0a0b0d" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content={site.name} />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={site.studio} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={photo} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={photo} />
    <script is:inline type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>
  <body>
    <a class="skip" href="#content">Skip to content</a>
    <Header />
    <main id="content">
      <slot />
    </main>
    <Footer />
  </body>
</html>
