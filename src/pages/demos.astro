---
import Base from '../layouts/Base.astro'
import { site } from '../data/site'
import { publicSurfaces, demoLinks, statusLabel } from '../data/products'

const rows = publicSurfaces().sort((a, b) => Number(b.status === 'live') - Number(a.status === 'live')).map((product) => ({ product, links: demoLinks(product) }))
---

<Base
  title={`Demos — ${site.studio}`}
  description="Working live labs (including Agent Fleet on Contabo/sslip), galleries, and downloads for Agentic Systems Studio. Scaffolds stay on GitHub, not this page."
>
  <section class="section">
    <p class="kicker">Demos</p>
    <h1 class="page-title mt-4">Open it. Try it. Inspect it.</h1>
    <p class="mt-5 max-w-2xl text-lg leading-relaxed text-mute">
      Start with a live product, download a local tool, or explore an early gallery. Each surface is clearly labelled so you know what to expect.
    </p>

    <div class="mt-12 space-y-4">
      {rows.map(({ product, links }, i) => (
        <article
          class="demo-row catalog-card has-accent panel p-5 sm:p-8"
          style={`--accent:${/^#[0-9a-fA-F]{6}$/.test(product.accent) ? product.accent : '#8b7cff'};--reveal-delay:${i % 3 * 70}ms`}
          data-reveal
        >
          <div class="flex flex-wrap items-center justify-between gap-3">
            <h2 class="display text-2xl text-paper">
              <a class="product-name-link" href={`/products/${product.slug}`}>{product.name}</a>
            </h2>
            <span class={`badge badge-${product.status}`}>{statusLabel(product.status)}</span>
          </div>
          <p class="mt-2 max-w-3xl text-sm leading-relaxed text-mute">{product.description}</p>
          <div class="mt-5 flex flex-wrap gap-2">
            {links.map((link) => (
              <a
                class:list={['btn', link.kind === 'live' || link.kind === 'download' ? 'btn-light' : 'btn-ghost']}
                href={link.href}
                rel="noreferrer"
                target="_blank"
              >
                {link.label} ↗
              </a>
            ))}
            <a class="btn btn-ghost" href={`/products/${product.slug}`}>Open product</a>
          </div>
        </article>
      ))}
    </div>
  </section>
</Base>
